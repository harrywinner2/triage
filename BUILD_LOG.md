# Build Log — Triage

One line per meaningful decision or phase transition. This is the running record so the run can proceed without interrupting the user.

- **Intake** — PRD: Replicated "Automated Technical Artifact Analysis". Decisions: web app form, deploy to Railway, synthesize a realistic support bundle. AI key not provided at intake → app architected dual-mode (deterministic detectors always on; OpenAI reasoning layer when `OPENAI_API_KEY` is set). Key flagged as the one thing needed before live demo/video.
- **Preflight** — git/gh(harrywinner2)/node all pass; railway authed (Harry W); vercel authed; ffmpeg present. WARN: no headless-browser MCP → Phase 2 style inferred, Phase 4 verify via HTTP + local Playwright, Phase 5 capture via local Playwright.
- **Phase 1** — Writing expanded PRD: product name "Triage", screen inventory, deterministic+LLM analysis pipeline, data model, AI surface.
- **Secrets** — User provided OpenAI key mid-run. Written to untracked `.env` (mode 600, git-ignored, verified via `git check-ignore`). `.env.example` tracked with placeholders. AI reasoning layer now live; will set as Railway secret at deploy. User to rotate after evaluation.
- **Browser** — User flagged that a controllable browser is essential for the demo. Installed Playwright + Chromium (+ headless shell, ffmpeg) locally. Used for live-verify and demo capture (no MCP needed).
- **Stack** — Node 20 + TypeScript + Express; deterministic detector catalog + OpenAI reasoning layer (fetch, no SDK dep); JSON-file store; single-file `public/index.html` frontend extended from the mockup. `tar` (system) for extraction, multer 2.x for uploads.
- **Phase 4b** — Engine complete. `tsc` clean; 13/13 vitest pipeline tests pass against the sample bundles. CLI verified end-to-end with the real key (correlated AI root cause + kubectl remediation). Tightened the logs permission pattern to avoid HTTP-403 false positives (10→9 findings on sample).
- **Phase 4c** — Public repo pushed: https://github.com/harrywinner2/triage (10 logical commits, secret_guard clean). Railway CLI `init` mutation kept timing out (non-TTY workspace prompt), so created project+service+vars via the Railway GraphQL API directly (key set as a hidden Railway variable, read from `.env`, never printed), then `railway link` + `railway up` (Docker build) succeeded. Live URL: https://triage-production-2f40.up.railway.app. Playwright verify: 10/10 checks pass against the live deployment with AI on. Captured live screenshots to design/.
- **Phase 5** — Demo video `demo/demo.mp4` (5m05s, 1920×1080 h264 + aac, 14.9MB). Narration via OpenAI TTS (onyx) from `demo/beats.json`; screen capture via Playwright against the live Railway URL (`scripts/capture-demo.mjs`), paced to narration durations; stitched with `assemble_video.sh` (ffmpeg). User asked for a 5-minute target (not 2); first cut was 3:40, expanded narration to land at 5:05.
- **Done** — All deliverables met: runnable public repo, live verified URL, `MY_APPROACH_AND_THOUGHTS.md` (493 words, ≤500), ~5 min demo video, human README. **Reminder: rotate the OpenAI key now that evaluation is set up.**
