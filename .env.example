# Copy to .env and fill in. The .env file is git-ignored and must never be committed.
# The app runs without a key (deterministic detectors only); set the key to enable the AI reasoning layer.

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
PORT=3000
